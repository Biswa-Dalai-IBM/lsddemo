service myFlow {
	FINALLY{
		INVOKE pub.math:addInts {
		    comment : "Adds two numbers"
		    input {
			    copy a -> num1;
			    copy b -> num2;
		 	 }
		 	output {
		    	copy result -> sum;
		  	}
	  	};
	}
	TRY {
		INVOKE pub.math:addInts {
		    comment : "Adds two numbers"
		    input {
			    copy a -> num1;
			    copy b -> num2;
		 	 }
		 	output {
		    	copy result -> sum;
		  	}
  		};
	}
	CATCH {
		INVOKE pub.math:addInts {
		    comment : "Adds two numbers"
		    input {
			    copy a -> num1;
			    copy b -> num2;
		 	 }
		 	output {
		    	copy result -> sum;
		  	}
	  	};
	}
	
}